name: CI - Unit Test

on:
  push:
  pull_request:
 
jobs:
  build:
 
    runs-on: ubuntu-latest
    steps:
      - name: 🛠️ Checkout Repository
        uses: actions/checkout@v4

      - name: ⚙️ Setup .NET 9 SDK
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 9.x

      - name: 📦 Restore Dependencies
        run: dotnet restore ./SophieHR.Api/SophieHR.Api.csproj

      - name: 🏗️ Build Project
        run: dotnet build ./SophieHR.Api/SophieHR.Api.csproj --no-restore

      - name: ✅ Run Tests
        run: dotnet test ./SophieHR.UnitTests/SophieHR.UnitTests.csproj --no-build --verbosity normal