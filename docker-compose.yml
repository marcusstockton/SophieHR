version: '3.4'

services:
  sophiehr.api:
    image: ${DOCKER_REGISTRY-}sophiehrapi
    build:
      context: .
      dockerfile: SophieHR.Api/Dockerfile
    ports:
      - "7189:443"
    depends_on:
      - db

  db:
    image: "mcr.microsoft.com/mssql/server:2022-latest"
    environment:
      - ACCEPT_EULA=Y
      - SA_PASSWORD=P@55w0rd123